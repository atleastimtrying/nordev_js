window.tl.Render = function(){
  // var render = function(event, milliseconds){
  //   if(milliseconds){
  //     $('.display').html(tl.util.parseTime(milliseconds));
  //   }
  // };
  // $(tl).on('render', render);
};