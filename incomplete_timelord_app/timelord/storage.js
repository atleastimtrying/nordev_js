window.tl.Storage = function(){
  // var storeTime = function(event, timeobject){
  //   localStorage.setItem('timelord_time', JSON.stringify(timeobject));
  // };
  
  // if(localStorage.getItem('timelord_time')){
  //   var timeobject = JSON.parse(localStorage.getItem('timelord_time'));
  //   tl.util.emit('startTime', timeobject);
  // }

  // $(tl).on('storeTime', storeTime);
};